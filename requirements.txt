Django==5.1.2                 # Exact version you are using
gunicorn>=21.2.0              # Production WSGI server (e.g., Heroku, Railway)
whitenoise>=6.6.0             # Serve static files in production
Pillow>=10.0                  # If you're using ImageField for story images
django-crispy-forms>=2.0      # Optional: for styled forms
